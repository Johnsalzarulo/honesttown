<div class="pheader">
	<div class="hero">
		<h1>Here’s what people are saying about <strong><%=@person.name%></strong>, <%=@person.location%>.</h1>
	</div>
</div>

<div class="maincontent">

	<%= render @person.sentences %>

<div class="addcard">
<h1>Add a card.</h1>
	<%= form_for([@person, @person.sentences.build]) do |f| %>
		<%= f.select(:subject, @subject.shuffle, {}, {:class => "select-style"}) %>
		<%= f.select(:verb, @verb.shuffle, {}, {:class => "select-style"}) %>
		<%= f.select(:adjective, @adjective.shuffle, {}, {:class => "select-style"}) %>
	    <%= f.submit %>
	<% end %>
 </div>


 </div>